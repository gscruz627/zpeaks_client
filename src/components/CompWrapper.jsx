import { Box } from "@mui/material";
import { styled } from "@mui/system";

const CompWrapper = styled(Box) ({
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between"
});

export default CompWrapper;
